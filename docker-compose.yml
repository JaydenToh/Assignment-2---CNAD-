services:
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - user-management

  user-management:
    build: ./user-management
    ports:
      - "4000:4000"

  exercise-management:
    build: ./exercise-management
    ports:
      - "5000:5000"

  clinic-video-management:
    build: ./clinic-video-management
    ports:
      - "6000:6000"
